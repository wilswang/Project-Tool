{
  "_README": [
    "這是範本檔，不含任何實際憑證，可以安全進版控。",
    "實際使用時：把 GCP 下載的 service account 金鑰另存為 service-account.json，",
    "放在 ProjectTool/config/（部署期）或 Project-Tool/src/config/（開發期）。",
    "service-account.json 已被兩個 repo 的 .gitignore 擋下，不會被 commit。",
    "取得金鑰後記得把 client_email 加為目標試算表的「檢視者」，否則 check-auth 會過但讀取會 403。",
    "建議 chmod 600 service-account.json。"
  ],
  "type": "service_account",
  "project_id": "<REPLACE_ME>",
  "private_key_id": "<REPLACE_ME>",
  "private_key": "<REPLACE_ME>",
  "client_email": "<REPLACE_ME>@<REPLACE_ME>.iam.gserviceaccount.com",
  "client_id": "<REPLACE_ME>",
  "auth_uri": "https://accounts.google.com/o/oauth2/auth",
  "token_uri": "https://oauth2.googleapis.com/token",
  "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
  "client_x509_cert_url": "<REPLACE_ME>",
  "universe_domain": "googleapis.com"
}
